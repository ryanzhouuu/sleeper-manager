"""Persistent state backends.

Import repositories from `sqlite`, `async_sqlite`, or `d1`. Protocols and
records live in `base`; shared DML is in `statements`; Mapping codecs are in
`rows`. Acknowledgement evidence SQL stays in `acknowledgements`.
"""
